問１ 正しいものはどれですか。モデルはListモデルとします。

a.すべてのデータを取り出すメソッドは、List.allメソッドである。

====
問2 ループ処理の正しい書き方はどれですか。

c.
<% @lists.each do |list| %>

タイトル

<%= list.title %>
<% end %>


===
問3 投稿データは、URLの/todolists/:idに含まれるidで判別するようにしました。この「:id」に入っている値をアクション内で取得するには、どれが正しいでしょうか。

d.
def show
@list = List.find(params[:id])
end


====
問4 記事一覧ページから詳細ページへのリンクを作成する方法は、どれが正しいでしょうか。

a.
<%=link_to 表示させるテキスト, "リンク先のURL" %>


